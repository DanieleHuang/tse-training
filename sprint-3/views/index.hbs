<!DOCTYPE html>
<html lang="en">

<head>
  <style>
    i {
      display: inline;
      margin: 0.3em;
    }

    .container {
      margin: 1em;
      width: 65em !important;
    }
    .card {
      width: 35em;
      padding: 0.5em 1.25em 0.25em 1.25em;
    }

    #retweet {
      font-size: 0.85em;
      margin: 0.7em auto 1.7em -0.2em;
      opacity: 0.7;
    }

    #icons {
      font-size: 0.85em;
      margin-top: -1em;
      margin-bottom: -0.3em;
      opacity: 0.7;
    }

    #user {
      font-size: 1.1em;
      margin-top: -0.7em;
      margin-bottom: 0.8em;
    }

    #tweet {
      font-size: 1.3em;
      margin-top: -0.7em;
      margin-bottom: 0.8em;
    }
  </style>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/all.css">
</head>

<body class="container" style="background: #fcfcfc">
    <div class="row">
      <div class="col-md-3">
        <img src="images/profile.jpeg" alt="gary">
      </div>
      <div class="col-md-9">
        {{#each chirps}}
          <div class="card">
            <p id="retweet"><i class="fas fa-retweet"></i>gary retweeted</p>
            <p id="user">{{author_name}} (@{{author_handle}})</p>
            <p id="tweet">{{text}}</p>
            <div class="row" id="icons">
              <p class="col-sm-2"><i class="far fa-comment"></i>{{comments}}</p>
              <p class="col-sm-2"><i class="fas fa-retweet"></i>{{retweets}}</p>
              <p class="col-sm-3"><i class="far fa-heart"></i>{{likes}}</p>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
</body>
</html>
